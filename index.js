
i = 0
let vet = []
do{
    let msg = Number(prompt('digite os numeros. (digite 0 para encerrar)'))
}
while (msg !==0) {
        msg = Number(prompt('digite os numeros. (digite 0 para encerrar)'))
         vet[i] = msg
         i++
}